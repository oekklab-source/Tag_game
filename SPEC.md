# 滑り台の動作仕様

既存のゲーム全体の仕様は README.md を参照。本書は滑り台モーションと出口の動作を定義する。

## 順方向

- 実際の斜面に足元が入ると約0.23秒で座り、足を前に伸ばして滑る。
- 斜面に沿ってモデルの向きを合わせる。視点を回しても滑走姿勢は走路に沿う。
- 下り加速と速度上限18m/sは維持する。滑走中に飛びつきは開始しない。
- 出口線から先は通常移動へ戻り、走りへの切り替えを行う。

## 出口への接近と逆走

- 出口先の平地区間では通常移動。Areaの外形に触れただけでは押し戻さない。
- 足元が斜面範囲内にあり、表面から下0.35m〜上0.75mの範囲で滑走を判定する。
- 出口から2m以内の斜面へ上向きの速度で入ると、約0.33秒だけ登る。
- 登る水平速度は6.0m/sから3.0m/sへ落ちる（最初の動きの2.0倍）。足の運びを1歩追加する。
  3m上昇/8m走路の実コリジョンテストでは水平約1.4mを目安（最初は約0.65m）。
- その後は前へ倒れて下り方向へ反転し、転倒動作も最初の2.0倍速とし、開始から約0.52秒でうつ伏せ滑走へ移る。
- うつ伏せでは頭を坂上、足を坂下へ向け、足から滑り戻る。
- うつ伏せでは両腕を外へ開いて顔との間隔を確保する。転倒終盤・回復開始も同じ姿勢へつなぐ。
- 出口線を越えた瞬間に滑走の移動拘束を解き、約0.267秒で手をついて起き上がる。
  起き上がりは見た目だけの状態で、移動入力を妨げず、追加のスタンを課さない。
- 上り入力を押し続けると、登る→転ぶ→滑り戻る→起き上がる→再び登る、と自然に繰り返す。
  入力の解除や一定距離離れる操作は不要。出口付近で上り入力を制限しない。
- 回復中も通常移動の目標速度へ40m/s²で近づけ、下りから上りへの急な速度反転を避ける。
  約0.267秒の回復中に斜面へ再接触しても転倒状態へ切り替えず、回復後に再挑戦する。
- 転倒中の移動も上りから下りへ補間し、進行方向を瞬間的に反転させない。
- ダイブで斜面に入った場合も滑走へ切り替え、一方通行を突破させない。

## 状態・同期・既存動作

- SlideRideがプレイヤー、鬼CPU、逃げ役CPUの状態と速度計算を共通化する。
- 同期値はphase、クリップ時刻、世界座標の向き、斜面角をVector4でまとめて送る。
  他ピアではこれを受けてアニメと向きを合わせる。通信版数はv5（リスポーン残り時間も同期）。
- 出口では即時解除。側面などAreaから離れた場合も解除する。
  検出の一時欠落には0.12秒の猶予を設ける。
- テレポート、ワープ、打ち上げ、バナナ転倒では滑走状態をリセットする。
- 既存のモデル、骨組み、走り、ダイブ、バナナ転倒、エモートは維持する。
  当たり判定カプセルは変更しない。

## アニメ資産と検証

- assets/character/fallguy.glb と tools/blender/fallguy.blend に5クリップを追加。
  SlideEnter / SlideSit / SlideReverseFall / SlideProne / SlideRecover。
- tools/blender/prototype_slide.py が承認済みポーズと接地補正を保持する。
  `--integrate` は既存資産の7クリップを残して滑走クリップを更新する。
  build_fallguy.py から全体を再生成した場合も同じ滑走クリップを生成する。
- tests/slide_gameplay.tscn: 実斜面・Player・Areaで接近、短い登り、うつ伏せ、即時操作復帰、
  順方向、連続逆走防止、既存クリップ、回復時間、CPUの共通処理を検証。
- 同テストの `-- slide-host` / `-- slide-client` はローカルの2プロセスを接続し、
  別ピアで5段階の状態と再生アニメが一致することを検証（テスト専用ENetポート19989）。
- tools/shot_slide.gd はGodotで取り込んだ姿勢を斜面上で描画する。
- 追いかけられている時の操作感と捕まりやすさは実プレイで最終確認する。

# 鬼側バナナの投げ仕様

- 水平投げ速度8.55m/s、上方向の初速12.11m/s、投げ専用重力14.65m/s²。
- キャラ前方1.2m・上方1.0mに生成する。
- 投げた瞬間の自キャラ速度を前後・左右100%、上下50%加算する。
- 静止時、同じ高さの平地で障害物がなければ、キャラ位置から着地点まで約16m、
  キャラの足元から最高点まで約6mを目安とする。物理更新の刻みによる誤差を含む。
- 前へ走りながら投げると遠くへ飛び、上昇中は高く、下降中は低い軌道になる。
- 逃走者の背後への設置、接触・転倒・同期・足場追従の動作は維持する。

# バナナの足場追従仕様

- プレゼントボックスは通り抜け可能なAreaであり、バナナの足場として扱わない。
- 通常の地面へ着地したバナナは、その場に固定する。
- リフトへ着地したバナナは、着地点の位置関係を保ってリフトと一緒に移動する。
- 回転床へ着地したバナナは、床上の位置関係を保って中心の周囲を移動する。
  バナナ自身の回転演出は維持する。
- 置き壁へ着地したバナナは、壁が消滅した時点で現在位置から再び重力落下する。
  足場消滅時に水平速度は加えない。
- 足場追従はサーバ側で処理し、既存の位置同期によって各クライアントへ反映する。

# 落下時のリスポーン仕様

- プレイヤー、CPU鬼、CPU逃走者が落下限界を下回った場合、落下したエリアに関係なく
  おしろ広場の中央 `(0, 3, 0)` へ復帰する。
- 中央に別のキャラクターがいる場合は、完全な重なりを避けるため、中央から半径2m以内の
  空いている候補位置へ復帰する。
- リスポーン時の速度、スタミナ、アイテム、各種一時状態のリセット処理は維持する。

# CPU逃走者デバッグの操作仕様

- CPU逃走者デバッグ中は、待機中・プレイ中・結果表示中を問わず次のキーを使用できる。
  `R`: バナナ、`T`: ブロック、`Y`: ロケット、`E`: 所持アイテムを使用する。
- `R` / `T` / `Y` は現在の所持品を上書きし、ルーレット待機なしですぐ使用可能にする。
  使用後も各キーを押せば何度でも取得できる。
- `Q` は通信状態とラウンド状態をリセットし、ゲームウィンドウを閉じずにメイン画面へ戻す。
- 操作説明はデバッグ中だけ画面左下の既存情報欄に小さく表示する。
- 通常モードではデバッグ用の取得・復帰操作を無効にし、`R` の役割選択を維持する。

# ダッシュパネルのゲージ表示仕様

- ダッシュパネルによるスピード上昇は、既存どおり速度1.6倍・効果時間2.5秒とする。
- スピードゲージはダッシュパネルに乗った直後を100%として表示し、2.5秒かけて0%まで減少する。
- 効果中に再びダッシュパネルへ乗った場合は、残り時間とゲージを100%へ戻す。

# ゲーム終了時の文言仕様

- 鬼が逃げ切られた場合は「にげきられた…」「逃げる人をつかまえられなかった」と表示する。
- 鬼が逃走者を捕まえた場合は「つかまえた！」「逃げる人をつかまえた」と表示する。
- 逃走者が逃げ切った場合は「にげきった！」「最後まで逃げきった」と表示する。
- 逃走者が捕まった場合は「つかまった…」「鬼につかまってしまった」と表示する。
- 逃走者が途中退出した場合の「ちゅうだん」表示は維持する。

- 落下復帰時だけ、立ち上がりを含む3秒間の移動不可ペナルティを開始する。
- 0〜2.7秒は座り、頭と上半身を円を描くように揺らす。頭上を黄色い小鳥3羽が周回する。
- 最後の0.3秒で立ち上がり、小鳥が消える。3秒後に通常操作へ戻る。
- 移動、ダッシュ、飛びつき、アイテム使用を制限する。視点操作と既存の接触判定は維持する。
- 通常のテレポートやラウンド開始では発動しない。無敵時間は追加しない。
- プレイヤー・CPU鬼・CPU逃走者で共通の残り時間を同期する（通信版数v5）。
- RespawnDizzyクリップを既存資産に追加。小鳥は scenes/respawn_birds.gd で描画する。
- tests/respawn_penalty.tscn で落下・3秒拘束・操作復帰・既存転倒・CPUを検証。
  `-- respawn-host` / `-- respawn-client` で別ピアの演出開始・終了も検証する。


# 外周の傾斜と巨大キャラ

- 外周の壁の総高は維持し、上面0.3mを1区間約8.1mの可動天面へ置き換える。4隅の面取り部分も対象。
- 外周に連続5秒乗ったキャラクターの足元区間が、予告なしで外向きに70度傾く。
- 外周上の区間をまたいでも経過時間を引き継ぐ。降りる・空中へ離れるとリセットする。
- 0.35秒で傾き、1秒時点から復元を始め、開始1.6秒で元の形に戻る。
- 内向き入力でも外へ落ちるよう外向き速度8m/sを1.2秒保持する。同区間に乗っていたキャラクターも対象。
- 落下後は既存の中央リスポーンと3秒の目回りにつながる。
- ホストが滞在時間を管理し、傾斜開始を全ピアへ同期。移動は各キャラクターの権威ピアで適用する（通信版数v6）。
- 黄色いバネの谷の南西角から外へ伸びる対角線上、約707m先の `(-580, -420, 580)` に既存キャラを240倍で1体配置する。顔は南西角へ向け、遠方の地平線付近に見える高さにする。全パーツを薄いグレーの無塗装風に統一。
- 巨大キャラは静止、当たり判定なし、影なし。黄色いバネの谷の南西角の外周に登ると顔が見える高さにする。
- tests/perimeter_rim.tscn で5秒・区間移動・復元・中央復帰・リセット・巨大キャラの設定を検証。
  `-- rim-host` / `-- rim-client` で通信先の傾斜開始と復元を検証する。
- tools/shot_perimeter.gd は外周上と地面からの巨大キャラの見え方を描画する。

- 外周四隅の対角壁の裏は三角柱で埋める。見た目と衝突形状を一致させ、角の空洞への落ち込みを防ぐ。三角形の天面にも5秒後の傾斜・外向き排出を適用する。
